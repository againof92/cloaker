# === Cloaker Rust - Environment Variables ===

# Porta do servidor (Render define PORT automaticamente)
PORT=8080

# Credenciais do admin
CLOAKER_ADMIN_USER=m4ciel
CLOAKER_ADMIN_PASS=linux0303

# URL do PostgreSQL (Render define DATABASE_URL automaticamente)
DATABASE_URL=postgres://user:password@host:port/dbname?sslmode=require

# Opcional: fallback
CLOAKER_DATABASE_URL=
